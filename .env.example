# =============================================================================
# Personal Website - 环境变量配置模板
# =============================================================================
# 使用说明：
#   1. 复制此文件为 .env: cp .env.example .env
#   2. 修改 .env 文件中的值
#   3. 确保 .env 在 .gitignore 中，不要提交到 Git！
# =============================================================================

# ==========================================
# 应用基础配置
# ==========================================
APP_NAME=PersonalSite
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:3000

# ==========================================
# 数据库配置 (PostgreSQL)
# ==========================================
# 开发环境默认使用 Docker Compose 中的 PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=personalsite
DB_USER=postgres
DB_PASSWORD=devpassword123

# 连接池配置
DB_POOL_MIN=5
DB_POOL_MAX=20

# ==========================================
# JWT 认证配置
# ==========================================
# 生成密钥命令: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-256-bits-long
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# ==========================================
# 前端配置
# ==========================================
# 开发环境 API 地址
VITE_API_BASE_URL=http://localhost:8080/api

# 应用信息
VITE_APP_TITLE=Personal Site
VITE_APP_DESCRIPTION=My personal website built with Vue3 + Spring Boot

# ==========================================
# 日志配置
# ==========================================
LOG_LEVEL=INFO
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=DEBUG

# ==========================================
# CORS 配置
# ==========================================
# 允许的前端域名，多个用逗号分隔
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# ==========================================
# 文件上传配置
# ==========================================
# 最大文件大小 (MB)
MAX_FILE_SIZE=10
MAX_REQUEST_SIZE=50

# 上传文件存储路径 (Docker 中使用卷)
UPLOAD_PATH=/app/uploads

# ==========================================
# 缓存配置
# ==========================================
# 启用 Spring 缓存
CACHE_ENABLED=true
CACHE_TYPE=caffeine

# ==========================================
# 生产环境特定配置 (GitHub Secrets)
# ==========================================
# 以下配置仅在 GitHub Actions 中使用，不要添加到 .env 文件！
# 在 GitHub Repository Settings → Secrets and variables → Actions 中配置：
#
# RAILWAY_TOKEN          - Railway API Token
# VERCEL_TOKEN           - Vercel API Token
# VERCEL_ORG_ID          - Vercel Organization ID
# VERCEL_PROJECT_ID      - Vercel Project ID
# VITE_API_BASE_URL      - 生产环境 API 地址
# TELEGRAM_BOT_TOKEN     - 通知机器人 Token (可选)
# TELEGRAM_CHAT_ID       - 通知群组 ID (可选)
